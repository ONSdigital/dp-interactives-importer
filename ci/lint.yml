---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-interactives-importer

run:
  path: dp-interactives-importer/ci/scripts/lint.sh